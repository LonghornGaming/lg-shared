[package]
name = "lg-shared"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_repr = "0.1"
time = { version = "0.3", features = ["serde"] }
